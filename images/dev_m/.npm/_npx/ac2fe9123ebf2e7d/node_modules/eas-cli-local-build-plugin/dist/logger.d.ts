/// <reference types="node" />
import { Writable } from 'stream';
import bunyan from '@expo/bunyan';
import { LogBuffer } from '@expo/build-tools';
interface CachedLog {
    msg: string;
    phase: string;
}
declare class BuildCliLogBuffer extends Writable implements LogBuffer {
    private readonly numberOfLines;
    writable: boolean;
    buffer: CachedLog[];
    constructor(numberOfLines: number);
    write(rec: any): boolean;
    getLogs(): string[];
    getPhaseLogs(buildPhase: string): string[];
}
export declare const logBuffer: BuildCliLogBuffer;
declare const defaultlogger: bunyan;
export default defaultlogger;
