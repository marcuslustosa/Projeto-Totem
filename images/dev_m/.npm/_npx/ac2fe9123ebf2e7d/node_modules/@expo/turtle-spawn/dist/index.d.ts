import { bunyan, PipeMode } from '@expo/logger';
import { SpawnResult, SpawnPromise, SpawnOptions as SpawnAsyncOptions } from '@expo/spawn-async';
declare type SpawnOptions = SpawnAsyncOptions & {
    logger?: bunyan;
    lineTransformer?: (line: string) => string | null;
    mode?: PipeMode;
};
declare function spawn(command: string, args: string[], _options?: SpawnOptions): SpawnPromise<SpawnResult>;
export default spawn;
export { SpawnOptions, SpawnResult, SpawnPromise };
