import { Ios } from '@expo/eas-build-job';
import { BuildContext } from '../../context';
export default class Keychain<TJob extends Ios.Job> {
    private readonly ctx;
    private readonly keychainPath;
    private readonly keychainPassword;
    private created;
    private destroyed;
    constructor(ctx: BuildContext<TJob>);
    get data(): {
        path: string;
        password: string;
    };
    create(): Promise<void>;
    importCertificate(certPath: string, certPassword: string): Promise<void>;
    ensureCertificateImported(teamId: string, fingerprint: string): Promise<void>;
    destroy(keychainPath?: string): Promise<void>;
    cleanUpKeychains(): Promise<void>;
    private findIdentitiesByTeamId;
}
