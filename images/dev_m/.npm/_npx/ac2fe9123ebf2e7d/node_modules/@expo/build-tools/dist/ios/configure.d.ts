import { Ios } from '@expo/eas-build-job';
import { BuildContext } from '../context';
import { Credentials } from './credentials/manager';
declare function configureXcodeProject(ctx: BuildContext<Ios.Job>, { credentials, buildConfiguration, }: {
    credentials: Credentials;
    buildConfiguration: string;
}): Promise<void>;
export declare function evaluateTemplateString(s: string, buildSettings: Record<string, string>): string;
export { configureXcodeProject };
